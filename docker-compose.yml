services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: xcompany
      POSTGRES_USER: xcompany
      POSTGRES_PASSWORD: xcompany_dev_change_me
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL","pg_isready -U xcompany -d xcompany"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://xcompany:xcompany_dev_change_me@db:5432/xcompany
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  postgres_data:
